name: Reusable Deploy
on: workflow_call # Allows this workflow to be called from other workfloews
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output info
        run: echo "Deploying and uploading..."  